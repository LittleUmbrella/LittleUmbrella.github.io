eaf.util.namespace('circleverse.viewModel');

//alert('hi');

//mostly a marker interface for now
circleverse.viewModel.ToolViewModel = (function () {

    var initSize = 60;

    return new JS.Module('circleverse.viewModel.ToolViewModel',  {
        

        initialize: function (object, parent, globalSettings, opts) {// (tracker, uri, templateUri, templateId, resultTemplateUri, callSpec, name, id, businessClass, opts) {
            var self = this;
            self.callSuper();
            
        }
            


    });
})();
